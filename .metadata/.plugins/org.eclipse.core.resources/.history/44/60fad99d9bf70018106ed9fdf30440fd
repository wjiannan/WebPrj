package com.cx.dao;

public interface ImessageDao {
	
	/*
	 * 获取最大的message ID
	 */
	Integer getMaxId();
	
	/*
	 * @param 
	 */
	void addMessage(Integer senderId, Integer recipientId, String content);
}
